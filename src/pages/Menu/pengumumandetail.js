import { View, Text } from 'react-native'
import React from 'react'
import { colors } from '../../utils'

export default function PengumumanDetail({navigation}) {
  return (
    <View style={{
        flex:1,
        backgroundColor:colors.white
    }}>
      <Text>PengumumanDetail</Text>
    </View>
  )
}